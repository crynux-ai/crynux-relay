---
environment: "release"
db:
  driver: "mysql"
  connection: "h_relay:hrelaypass@(mysql:3306)/h_relay"
log:
  level: "info"
  output: "/app/data/logs/h_relay.log"
  max_file_size: 100
  max_days: 30
  max_file_num: 5
http:
  host: "0.0.0.0"
  port: "8080"
data_dir:
    inference_tasks: "/app/data/inference_tasks"
