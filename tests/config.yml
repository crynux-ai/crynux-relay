---
environment: "test"
db:
  driver: "sqlite"
  connection: "/h_relay/data/test_db.sqlite"
log:
  level: "debug"
  output: "stdout"
  max_file_size: 100
  max_days: 30
  max_file_num: 5
http:
  host: "127.0.0.1"
  port: "8080"
data_dir:
    inference_tasks: "/h_relay/data/inference_tasks"
